# Drawer v2 重构计划

## 1. 决策摘要

Drawer v2 是面向 Android 与 Windows 的本地照片整理应用。正式客户端统一采用 **Compose Multiplatform**：Android 使用 Jetpack Compose，Windows 使用 Compose Desktop。

- 不再以 Flutter、React Native TurboModule 或 Ktor HTTP 作为客户端交付路径。
- Kotlin 业务层直接被客户端调用；Ktor 仅保留为归档/实验代码，不在主构建与 CI 中维护。
- Flutter 的本地照片索引不迁移。v2 使用独立本地数据库，首次运行由用户重新选择目录。
- 平台范围：Android API 26+，Windows 10/11 x64。
- Android 仅支持本机内部存储与 SD 卡的 SAF 目录授权，不支持网盘等文档提供者。

## 2. v1 完成标准

Android 与 Windows 均应支持：

1. 选择一个或多个源目录，以及恰好一个目标目录；配置在重启后仍保留。
2. 递归扫描源目录中的 JPEG/JPG、PNG、WebP、HEIC/HEIF、GIF、BMP。
3. 扫描目标目录时完整跳过目标子树；跳过 `Drawer Trash` 受管目录。
4. 后台流式扫描：发现照片后即可处理；已发现队列按文件修改时间倒序。新发现的更晚照片可插入队列前端，但不打断当前照片。
5. 展示照片和可获得的元信息。必须展示文件修改时间；EXIF 拍摄时间仅在存在时展示；Android 无法获得文件创建时间时显示“不可用”。
6. 用户点击分类标签后，立即将照片移动到目标根目录下对应分类目录。
7. 支持创建分类标签；分类标签就是目标根目录的直接子目录，按首次创建或首次扫描发现时间倒序显示。
8. 支持跳过照片、冲突处理、同一应用进程内的撤销、崩溃恢复及回收站统计/清空。

v1 不包含应用内分类重命名、删除或回收站恢复；这些进入 v2。用户可通过文件管理器直接操作目标目录或 `Drawer Trash`，应用在下次同步时以真实目录为准。

## 3. 文件与目录规则

### 3.1 源目录与目标目录

- 源目录必须至少一个；目标目录必须恰好一个。
- 若候选源目录被一个已接受源目录包含，静默忽略该候选目录。例如已选择 `DCIM` 后选择 `DCIM/Camera`，后者不写入配置。
- 目标目录是源目录的子目录时允许，但扫描必须跳过该目标子树。
- 源目录位于目标目录内时拒绝该配置，因为源会被完整排除。
- Windows 递归扫描不跟随符号链接、junction 或其他 reparse point。
- 应用从不自动删除空源目录，也不改变照片内容、EXIF 或格式。唯一允许的内容外改变是重名处理时由用户确认的新文件名。

### 3.2 分类目录

- 目标根目录的直接子目录是分类标签的唯一存在事实来源。
- 标签顺序使用持久化的 `first_seen_at`：应用创建标签时立即写入；外部创建的目录在首次扫描到时写入。
- 外部改名表现为旧标签消失、新标签首次发现，因此新名称排到最左。
- 标签名不做静默替换。拒绝路径分隔符、`\\ / : * ? \" < > |`、Windows 设备名、尾部空格/点和空名称。
- 标签与重名判断均使用不区分大小写的跨平台比较；`Family` 与 `family` 视为冲突。
- `Drawer Trash` 是保留名称，不能作为分类标签，永远排除在分类同步与扫描外。

### 3.3 媒体与扫描错误

- v1 不做内容去重；只有同一目标分类下的同名文件才进入冲突处理。
- GIF 以静态首帧预览。
- RAW 与 AVIF 不在 v1 支持范围。
- 文件扩展名/魔数符合格式但无法解码的图片不可移动，记录为本次扫描的“无法读取”。
- 权限拒绝、目录临时不可读等扫描错误不阻断其他目录，不被持久化为“跳过”；完成摘要应列出错误数量与受影响路径。

## 4. Session、队列与状态

一个 session 是一次应用进程生命周期。扫描队列和撤销栈只存在于内存中；应用关闭后两者失效。

- 扫描没有独立的“停止”按钮。用户切换源/目标目录时，应用提示该切换会中断扫描；确认后取消旧枚举、丢弃未处理队列、保存新配置并重新扫描。
- 目录切换不能中断正在执行的文件操作；须等待其成功、失败或进入“需要用户介入”。
- 已成功移动的操作形成 LIFO 撤销栈。撤销成功后，照片立即放回待处理队列最前面。
- 跳过不是撤销操作。
- 队列为空而扫描仍在进行时显示“继续扫描中”；扫描完成且队列为空时显示本 session 摘要：已移动、跳过、已保留副本、无法读取数量。

## 5. 持久状态

使用新的 SQLDelight SQLite 数据库；不复用 Flutter 或当前 JDBC `core-db` schema。

| 数据 | 语义 |
| --- | --- |
| `source_roots` | 已接受的源目录引用、展示名称、授权/可用状态 |
| `current_target` | 当前唯一目标目录引用与展示名称 |
| `known_trash_roots` | 曾作为目标使用过的位置及其 `Drawer Trash` 目录，用于跨目标统计/清空 |
| `category_first_seen` | 目标目录下标签的首次创建/发现时间 |
| `suppressed_items` | `skipped` 或 `kept_copy` 的文件指纹记录 |
| `operation_journal` | 唯一一条未完成文件操作的恢复状态 |

文件的持久引用必须是平台可重新解析的 opaque reference。业务层不解释其内容；Windows NIO 和 Android SAF 适配器各自解析它。

### 5.1 跳过与已保留副本

- `skipped`：用户暂不处理。设置中的“清除跳过”删除该记录并重新扫描，使照片回到待处理队列。
- `kept_copy`：目标副本已校验成功，但源删除失败后用户选择保留两份。后续扫描不再重复展示它。
- 两种记录均以“源根 + 文件引用 + 文件大小 + 修改时间”识别；大小或修改时间变化即视为新文件。
- 设置提供“重新检查已保留副本”，删除该类记录并重新扫描。

### 5.2 回收站

每个目标根下存在一个用户可见、应用保留的 `Drawer Trash/` 目录。覆盖操作先将旧目标安全移入该目录，再尝试将新照片移动到目标位置。

- 应用关闭不清空回收站；应用内不提供恢复。
- 用户可用文件管理器从该目录手动恢复文件。
- 设置页聚合所有已知回收站位置，显示照片数量和占用空间。
- 旧目标不可访问时，保留记录并显示“位置不可访问、无法统计/清空”。
- “清空回收站”显示数量和大小，要求勾选高风险确认后才启用永久删除。中断或失败时保留未删除内容，允许下次继续。

## 6. 移动、冲突与崩溃恢复

### 6.1 基本移动

文件操作单线程串行执行。绝不将完整照片加载进内存。

```text
创建目标临时文件
  -> 流式复制源文件
  -> 校验大小（可选内容摘要）
  -> 形成最终目标文件
  -> 删除源文件
  -> 标记完成
```

每个阶段都更新唯一的 `operation_journal`。任何失败都必须保证源文件仍存在或已经有经验证的最终目标文件；不得出现“源已删除、最终目标不存在”。启动时优先恢复该日志，恢复完成前不得开始新的扫描或文件操作。

### 6.2 同名冲突

冲突页同时展示源/目标照片以及文件名、文件大小、分辨率、拍摄时间（若有）、修改时间和可获得的创建时间。

允许操作：

- 保留目标并跳过当前源文件；
- 保留两者：使用默认的 `文件名 (1).扩展名`，允许用户编辑后移动；
- 覆盖目标：先进入二次确认弹窗，用户勾选“我理解这会替换目标照片”后才能确认。

覆盖按“移动 + 删除”实现：旧目标先进入 `Drawer Trash`；若此步骤失败，新照片不开始移动。旧目标留在回收站直到用户手动清空，因此 session 内撤销可恢复它。

### 6.3 删除源失败

删除源文件自动重试 3 次。仍失败时暂停在当前照片并提供：

1. 重试删除源文件；
2. 安全回退本次移动：移除新目标；若覆盖过则恢复回收站中的旧目标；
3. 保留两份并标记为 `kept_copy`，继续下一张。

撤销前必须重新校验目标文件的大小与修改时间。目标不存在或已被外部替换时停止撤销并提示用户介入，绝不操作可能不同的文件。

## 7. 模块边界

```text
:domain                 共享模型、规则、SessionCoordinator
:storage-contract       可持久 opaque 引用、流式读写与元数据能力契约
:storage-transaction    移动事务、冲突、回收站、恢复、撤销
:persistence            SQLDelight schema、查询与仓储
:storage-nio            Windows NIO 实现
:storage-saf            Android SAF 实现
:compose-ui             共享 Compose UI 和状态展示
:android-app            Android 入口、目录选择、持久 URI 授权
:desktop-app            Windows 入口、目录选择与打包
:testkit                假文件系统、故障注入与契约测试
```

旧 `core-scanner`、`core-fileops`、`core-db` 只作为行为和测试素材参考，不直接迁入新客户端：它们是 JVM 模块，缺少可持久引用/元信息/流式 IO，并且现有 `FileOps.atomicMove` 在最终目标可见前删除源文件。

## 8. 分阶段实施

### Phase 0 — 隔离旧交付路径

1. 将 Ktor 服务端与 React Native TurboModule 移出主构建、主 CI。
2. 保留 Flutter 与旧 Kotlin 代码作为只读行为参考，避免继续添加业务功能。
3. 新 CI 不以旧 Kotlin 单测绿色作为 App 可交付证明。

### Phase 1 — 新工程与持久化骨架

1. 建立 Compose Multiplatform、Android、Windows 与 SQLDelight 模块。
2. 实现配置、回收站根、标签首次发现、跳过/已保留副本、单行日志 schema。
3. 实现启动恢复门：日志未恢复前禁止新操作。

### Phase 2 — 存储契约与安全事务

1. 定义可持久 opaque 文件引用、目录列举、流式读写、创建文件、删除、移动、元信息与能力查询契约。
2. 实现 NIO 适配器，显式跳过 reparse point。
3. 实现 SAF 适配器，持久化 URI 授权，只接受本地/SD 存储提供者。
4. 实现正常移动、覆盖进回收站、三次重试、回退与启动恢复。

### Phase 3 — 扫描与 Session 协调器

1. 递归、多源、目标子树/回收站排除、格式识别和扫描错误事件。
2. 后台流式发现与修改时间排序队列。
3. 跳过/已保留副本过滤、内存撤销栈、目录切换受控中断。

### Phase 4 — Compose v1 UI

1. 源/目标目录配置与 Android 权限失效提示。
2. 照片预览、元信息、横向标签栏、就地新建标签、跳过。
3. 冲突双照片比较与覆盖二次确认。
4. 撤销、扫描/完成摘要、需要用户介入状态。
5. 设置：清除跳过、重新检查已保留副本、回收站统计和高风险清空。

### Phase 5 — 端到端验证与切换

1. Windows NIO 集成测试与 Android SAF 仪器测试。
2. Android 真机/模拟器测试：持久 URI 授权、权限撤销、SD 卡不可用、跨卷复制。
3. GitHub Actions 的普通 `push` 与 `pull_request` 必须执行共享单测、Windows NIO 契约测试、Windows 客户端编译和 Android debug 编译；CI 不再把旧 Flutter 或旧 `kotlin-core` 的绿色结果视作新客户端验收。
4. 推送版本标签（`v*`）时，在上述验证通过后构建 Windows x64 MSI 与 Android release APK/AAB，并将产物附到对应 GitHub Release。Android 发布签名使用 GitHub Secrets 注入；缺少签名材料时发布工作流必须显式失败，不能悄悄上传 debug 包冒充发布版。Windows 代码签名列为发布前置条件，证书未配置时产物标为未签名。
5. 以新客户端的纵切验收取代 Flutter smoke test；通过后才删除或彻底归档旧交付路径。

## 9. 必测故障矩阵

- 复制、校验、最终落盘、删源的每个阶段分别失败或崩溃。
- 覆盖前旧目标入回收站失败；覆盖后源删除失败；撤销覆盖。
- 重名的跳过、重命名、覆盖二次确认。
- 目标在源目录内部、源目录在目标内部、多源重叠。
- 空间不足、权限撤销、SD 卡拔出、外部替换/删除待处理或待撤销文件。
- 重启时仅保留最后一个未完成操作的恢复日志。
- 清空回收站中途失败后重新执行。

## 10. v2 候选项

- 应用内重命名与删除分类标签。
- 应用内浏览和恢复回收站。
- RAW、AVIF、更多格式与更丰富的 EXIF/媒体元信息。
- 跨设备同步、局域网 API 或 Ktor 服务端重新引入。
